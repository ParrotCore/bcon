use "capital" as capital;
use "capital-of-region" as regionalCapital;
use "important-city" as importantCity;
use "city" as city;
use "town" as town;
use "country" as country;

export [
    @* => capital;
    @* => regionalCapital;
    @* => importantCity;
    @* => city;
    @* => town;
    @* => country;
];