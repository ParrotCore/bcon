use [
    @* => "capital";
    @* => "regional-capital";
    @* => "important-city";
    @* => "city";
    @* => "town";
    @* => "country";
] as cityTypes;

use [
    capital;
    regionalCapital;
    importantCity;
    city;
    town;
    country;
] from cityTypes;

export [
    @* => capital;
    @* => regionalCapital;
    @* => importantCity;
    @* => city;
    @* => town;
    @* => country;
];