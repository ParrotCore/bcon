# Przykład z luźnymi wyrażeniami w BCON
# Przydatne podczas rozwoju i debugowania

# Tymczasowe wartości testowe (ignorowane)
"TODO: update this config";
42;
[@debug => True;];

# Właściwa konfiguracja
use "localhost" as host;
use 8080 as port;

# Zakomentowane stare wartości można pozostawić jako wyrażenia
# 3000;  # stary port
# "0.0.0.0";  # stary host

use [@* => "redis"; @* => "postgres"; @* => "mongodb";] as databases;

# Debugowe wartości (można zostawić)
999;
"checkpoint";

export [
    @host => host;
    @port => port;
    @databases => databases;
];
